import { ImpureExamplePipe } from './impure-example-pipe';

describe('ImpureExamplePipe', () => {
  it('create an instance', () => {
    const pipe = new ImpureExamplePipe();
    expect(pipe).toBeTruthy();
  });
});
